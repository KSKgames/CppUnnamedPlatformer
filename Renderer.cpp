#include"Renderer.h"
void RenderLevelUsingCamera(sf::RenderWindow& window, gm::camera& cam, gm::level& level)
{
	/*for (int i = 0; i < level.objectRefs.size(); i++) {
		if(level.objectRefs[i].pos.x<cam.position.x+cam.getSize().x)
			
	}*/
}